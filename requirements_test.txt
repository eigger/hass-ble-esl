# Test environment, pinned so CI is reproducible and Dependabot can bump it.
# The integration's own requirements come from manifest.json and the Home
# Assistant component requirements from scripts/ha_component_requirements.py
# (see .github/workflows/tests.yml).
pytest-homeassistant-custom-component==0.13.366
bluetooth-sensor-state-data==1.9.0
sensor-state-data==2.20.0
